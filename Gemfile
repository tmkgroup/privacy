source "https://rubygems.org"

# Jekyll itself. Cloudflare Pages installs the gems listed here
# automatically before running `jekyll build`.
gem "jekyll", "~> 4.3"

# Plugins are intentionally minimal. We avoid GitHub Pages-style
# whitelist plugins so the site stays portable to any static host.
group :jekyll_plugins do
  # (none today — kept here as the canonical place to add plugins later)
end
